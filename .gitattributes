# Normalize all text files to LF in the repo and on checkout (Windows + Linux
# dev container share one working tree, so line endings must not drift).
* text=auto eol=lf

# Windows batch scripts must stay CRLF.
*.bat text eol=crlf

gradlew text eol=lf
*.jar binary
